services:
  - type: web
    name: xiaoma-mahjong
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python cloud_mahjong_server.py --host 0.0.0.0 --port $PORT"
